%nav.top-bar{"data-topbar" => ""}
  %ul.title-area
    %li.name
      %h1
        %a{href: root_path} Home
  - if user_signed_in?
    %ul.left
      %li
        %a{href: current_user}= current_user.username
      %li
        %a My Saved Palettes
  - else
    %section.top-bar-section
      %ul.right
        %li.has-form
          = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
            .row.collapse
              .small-5.columns
                =f.text_field :login, autofocus: 'true', placeholder: 'Username'
                -#<%= f.text_field :login, autofocus: true, placeholder: "Username" %>

              .small-5.columns
                -#<%= f.password_field :password, autocomplete: "off", placeholder: "Password" %>
                =f.password_field :password, autocomplete: 'off', placeholder: 'password'

              .small-2.columns
                -#<%= f.submit "Sign in", class: "button postfix" %>
                = f.submit 'Sign in', class: 'button expand'
-#
  <% if devise_mapping.rememberable? -%>
    <div class="small-2 columns"><%= f.check_box :remember_me %> <%= f.label :remember_me %></div>
  <% end -%>
  -->


<!--<%= render "devise/shared/links" %>-->
